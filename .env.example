# Copy to .env.local for local dev, and set these in Vercel for production.

# --- Email lead delivery (Resend — free tier at resend.com) ---
RESEND_API_KEY=
LEAD_TO_EMAIL=
LEAD_FROM_EMAIL=

# --- SageFlow mirror (leave blank until SageFlow has an intake endpoint) ---
SAGEFLOW_WEBHOOK_URL=
